<div class="col-md-{{ $size }} mb-3">
    <label for="{{ $name }}">{{ $label }}</label>
    <input type="{{ $type }}" class="form-control" id="{{ $name }}" 
    placeholder="Masukkan {{ $label }}" name="{{ $name }}" value="{{ $value }}">
</div>